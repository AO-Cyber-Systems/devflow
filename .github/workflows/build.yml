name: Build

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      build_release:
        description: 'Build release artifacts'
        required: false
        default: 'false'
        type: boolean

env:
  NODE_VERSION: "20"
  PYTHON_VERSION: "3.11"
  TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
  TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

jobs:
  build-python:
    name: Build Python Package
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Check package
        run: twine check dist/*

      - name: Upload Python artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: dist/

  build-tauri:
    name: Build Tauri (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - platform: macos-intel
            os: macos-15-large
            target: x86_64-apple-darwin
          - platform: macos-arm
            os: macos-latest
            target: aarch64-apple-darwin
          - platform: windows
            os: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ui/package-lock.json

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            ui/src-tauri/target/
          key: ${{ matrix.os }}-cargo-build-${{ hashFiles('ui/src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ matrix.os }}-cargo-build-
            ${{ matrix.os }}-cargo-

      - name: Install Linux dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev

      - name: Install frontend dependencies
        working-directory: ui
        run: npm ci

      - name: Build Tauri app
        working-directory: ui
        run: npm run tauri:build -- --target ${{ matrix.target }}

      - name: Upload Linux artifacts
        if: matrix.platform == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: tauri-linux
          path: |
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage.sig

      - name: Upload macOS artifacts
        if: startsWith(matrix.platform, 'macos')
        uses: actions/upload-artifact@v4
        with:
          name: tauri-${{ matrix.platform }}
          path: |
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg.sig
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app.tar.gz
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app.tar.gz.sig

      - name: Upload Windows artifacts
        if: matrix.platform == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: tauri-windows
          path: |
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi.sig
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe
            ui/src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe.sig

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-python, build-tauri]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Delete existing release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ github.ref_name }}
        run: |
          # Delete any existing release for this tag to avoid asset accumulation
          # This ensures we get a clean release with only the current build's artifacts
          if gh release view "$TAG_NAME" > /dev/null 2>&1; then
            echo "Deleting existing release for $TAG_NAME"
            gh release delete "$TAG_NAME" --yes || true
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f | head -50

      - name: Flatten artifacts
        run: |
          mkdir -p release-assets

          # Flatten Linux artifacts
          find artifacts/tauri-linux -type f \( -name "*.deb" -o -name "*.AppImage" -o -name "*.AppImage.sig" \) -exec cp {} release-assets/ \; 2>/dev/null || true

          # Flatten macOS Intel artifacts (rename .app.tar.gz to include architecture)
          find artifacts/tauri-macos-intel -type f -name "*.dmg" -exec cp {} release-assets/ \; 2>/dev/null || true
          find artifacts/tauri-macos-intel -type f -name "*.dmg.sig" -exec cp {} release-assets/ \; 2>/dev/null || true
          for f in $(find artifacts/tauri-macos-intel -type f -name "*.app.tar.gz" ! -name "*.sig"); do
            cp "$f" "release-assets/DevFlow_x64.app.tar.gz"
          done 2>/dev/null || true
          for f in $(find artifacts/tauri-macos-intel -type f -name "*.app.tar.gz.sig"); do
            cp "$f" "release-assets/DevFlow_x64.app.tar.gz.sig"
          done 2>/dev/null || true

          # Flatten macOS ARM artifacts (rename .app.tar.gz to include architecture)
          find artifacts/tauri-macos-arm -type f -name "*.dmg" -exec cp {} release-assets/ \; 2>/dev/null || true
          find artifacts/tauri-macos-arm -type f -name "*.dmg.sig" -exec cp {} release-assets/ \; 2>/dev/null || true
          for f in $(find artifacts/tauri-macos-arm -type f -name "*.app.tar.gz" ! -name "*.sig"); do
            cp "$f" "release-assets/DevFlow_aarch64.app.tar.gz"
          done 2>/dev/null || true
          for f in $(find artifacts/tauri-macos-arm -type f -name "*.app.tar.gz.sig"); do
            cp "$f" "release-assets/DevFlow_aarch64.app.tar.gz.sig"
          done 2>/dev/null || true

          # Flatten Windows artifacts
          find artifacts/tauri-windows -type f \( -name "*.msi" -o -name "*.msi.sig" -o -name "*.exe" -o -name "*.exe.sig" \) -exec cp {} release-assets/ \; 2>/dev/null || true

          # Copy Python artifacts
          cp artifacts/python-dist/* release-assets/ 2>/dev/null || true

          echo "Flattened release assets:"
          ls -la release-assets/

      - name: Generate latest.json for auto-updater
        env:
          GITHUB_REF: ${{ github.ref }}
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"

          # Read signature files from flattened release-assets directory
          LINUX_SIG=$(cat release-assets/*.AppImage.sig 2>/dev/null | tr -d '\n' || echo "")
          MACOS_INTEL_SIG=$(cat release-assets/DevFlow_x64.app.tar.gz.sig 2>/dev/null | tr -d '\n' || echo "")
          MACOS_ARM_SIG=$(cat release-assets/DevFlow_aarch64.app.tar.gz.sig 2>/dev/null | tr -d '\n' || echo "")
          WINDOWS_SIG=$(cat release-assets/*_x64-setup.exe.sig 2>/dev/null | tr -d '\n' || echo "")

          cat > release-assets/latest.json << EOF
          {
            "version": "${VERSION}",
            "notes": "See release notes at https://github.com/AO-Cyber-Systems/devflow/releases/tag/v${VERSION}",
            "pub_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "platforms": {
              "linux-x86_64": {
                "url": "https://github.com/AO-Cyber-Systems/devflow/releases/download/v${VERSION}/DevFlow_${VERSION}_amd64.AppImage",
                "signature": "${LINUX_SIG}"
              },
              "darwin-x86_64": {
                "url": "https://github.com/AO-Cyber-Systems/devflow/releases/download/v${VERSION}/DevFlow_x64.app.tar.gz",
                "signature": "${MACOS_INTEL_SIG}"
              },
              "darwin-aarch64": {
                "url": "https://github.com/AO-Cyber-Systems/devflow/releases/download/v${VERSION}/DevFlow_aarch64.app.tar.gz",
                "signature": "${MACOS_ARM_SIG}"
              },
              "windows-x86_64": {
                "url": "https://github.com/AO-Cyber-Systems/devflow/releases/download/v${VERSION}/DevFlow_${VERSION}_x64-setup.exe",
                "signature": "${WINDOWS_SIG}"
              }
            }
          }
          EOF

          echo "Generated latest.json:"
          cat release-assets/latest.json

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          generate_release_notes: true
          files: release-assets/*
